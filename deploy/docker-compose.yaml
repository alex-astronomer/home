version: '3.3'

services:
  deploy:
    build: .
    volumes:
      - .:/deploy/scripts
      - ../lib:/deploy/lib
      - ../home.ino:/deploy/home/home.ino
      - ../build:/deploy/build
    command: python /deploy/scripts/deploy.py
    privileged: true
    network_mode: host
